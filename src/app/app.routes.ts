import { Routes } from '@angular/router';
import { HomeComponent } from './pages/home/home'; // Verifica il percorso

export const routes: Routes = [
  { path: '', component: HomeComponent }, // La pagina iniziale è Home
];